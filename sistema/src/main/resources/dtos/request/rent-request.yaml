components:
  schemas:
    RentRequest:
      type: object
      required:
        - contractId
        - automobileId
        - price
        - startDate
        - endDate
        - requestType
      properties:
        contractId:
          type: integer
          format: int64
        automobileId:
          type: integer
          format: int64
        price:
          type: number
          format: float
          minimum: 10
        startDate:
          type: string
          format: date
        endDate:
          type: string
          format: date
        requestType:
          type: string
          enum: [CLIENTS, BANKS, ENTERPRISE]