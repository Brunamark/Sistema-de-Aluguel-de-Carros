components:
  schemas:
    Contract:
      type: object
      properties:
        id:
          type: integer
          format: int64
        creditQuantity:
          type: integer
        contractType:
          type: string
          enum: [SIMPLE, CREDIT]